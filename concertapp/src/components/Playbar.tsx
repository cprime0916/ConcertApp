import React from "react";
interface PlaybarProps {
    AudioPath: string;
}

const Playbar: React.FC<PlaybarProps> = ({ AudioPath }: { AudioPath: string }) => {
    return (
        <>
            { /* TODO: construct Playbar component */ }
        </>
    );
};

export default Playbar;